<ui:FluentWindow x:Class="TailorMail.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="确认" Height="Auto" Width="380"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" MinWidth="300" MinHeight="120"
        FontFamily="{StaticResource UIFontFamily}"
        Background="{StaticResource SurfaceBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="MessageText"
                   Style="{StaticResource BodyTextStyle}" Margin="0,0,0,16"
                   TextWrapping="Wrap" MaxHeight="200"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <ui:Button x:Name="BtnNo" Content="否" Click="BtnNo_Click" Padding="16,6" Margin="0,0,8,0"/>
            <ui:Button x:Name="BtnYes" Content="是" Click="BtnYes_Click" Padding="16,6" Appearance="Primary"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
